version: "3"
services:


  web:
    build: ./frontend
    depends_on:
      - api
    ports:
      - "8081:8081"
    networks:
      - backend
      
      
  api:
    build: ./backend
    depends_on:
      - mongo
    ports:
      - "36150:36150"
    networks: 
     - backend
  
  
  mongo:
    image: mongo
    restart: always
    volumes: 
      - mongo:/data/mongo
    environment: 
      MONGODB_INITDB_ROOT_USERNAME: nico
      MONGODB_INITDB_ROOT_PASSWORD: korczak
    networks: 
     - backend

networks:
  backend:

volumes: 
  mongo:
